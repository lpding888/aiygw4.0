* [ ] PM2 配置（cluster=3）与日志轮转完成。
* [ ] Nginx 反代/健康检查/证书配置完成。
* [ ] `.env` 从 `shared/` 引入，**不打包**到 release。
* [ ] `release.sh/rollback.sh` 可执行，≤ 3 分钟回滚。
* [ ] 发布后冒烟脚本通过。
* [ ] 版本/commit/checksum/发布时间已记录。
* [ ] 磁盘空间>20%，日志轮转启用。
* [ ] 非 root 账户运行 Node。
* [ ] 如果有迁移：迁移脚本已独立执行并有回滚计划。
* **反例**：失败后找不到上一个版本；health 未通过仍切流；缺少冒烟脚本。
