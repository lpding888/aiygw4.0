# ===========================================
# AI照 Redis 配置（持久化 + BullMQ 专用）
# ===========================================

appendonly yes
appendfsync everysec

save 900 1
save 300 10
save 60 10000

dir /data

maxmemory-policy noeviction
timeout 0
tcp-keepalive 300

lua-time-limit 5000

notify-keyspace-events Ex
