# Docker开发环境配置

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=dev_password_123
DB_NAME=ai_photo
DATABASE_POOL_MIN=5
DATABASE_POOL_MAX=20

# JWT配置
JWT_SECRET=docker_dev_secret_key_for_testing_only_32chars
JWT_EXPIRE=7d

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 加密配置
CREDENTIALS_ENCRYPTION_KEY=docker_dev_encryption_key_32ch

# 内部回调签名密钥
INTERNAL_CALLBACK_SECRET=docker_dev_callback_secret_key

# 服务器配置
PORT=3000
NODE_ENV=development
API_DOMAIN=http://localhost:3000

# 日志配置
LOG_LEVEL=debug

# ==========================================
# ⚠️ 本地开发环境配置 - 使用MinIO模拟COS
# ⚠️ 部署到服务器时需要改为真实的腾讯云COS配置
# ==========================================

# MinIO本地对象存储配置 (模拟腾讯云COS)
# 部署时改为真实配置: TENCENT_SECRET_ID, TENCENT_SECRET_KEY, TENCENT_APPID
TENCENT_SECRET_ID=minioadmin
TENCENT_SECRET_KEY=minioadmin123
TENCENT_APPID=1234567890
COS_BUCKET=ai-photo-dev
COS_REGION=local
COS_IMAGE_DOMAIN=http://localhost:9000

# RunningHub配置 (测试环境使用假数据)
# 部署时改为真实配置
RUNNINGHUB_API_KEY=test_runninghub_key
RUNNINGHUB_WEBAPP_ID=test_webapp_id
RUNNINGHUB_BASE_URL=https://api.runninghub.com
RUNNINGHUB_TIMEOUT=600000

# 会员配额配置
PLAN_MONTHLY_QUOTA=100
MEMBERSHIP_PRICE=9900
MEMBERSHIP_DURATION_DAYS=30

# 配额成本配置
QUOTA_COST_BASIC_CLEAN=1
QUOTA_COST_MODEL_POSE12=1
QUOTA_COST_VIDEO_GENERATE=5

# 内容审核配置 (本地开发关闭)
AUDIT_ENABLED=false
AUDIT_THRESHOLD=0.8
