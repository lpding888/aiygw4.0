# -------------------------
# Node / JavaScript
# -------------------------
# 依赖
node_modules/

# 注意：推荐将 lock 文件提交到仓库以保证可复现构建（因此不在此处忽略）
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# npm / yarn debug 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# -------------------------
# 环境与密钥（不要提交）
# -------------------------
.env
.env.*.local
.env.local
.env.production
.env.development
.env.test

# 项目私有配置（可能包含密钥）
project.private.config.json

# -------------------------
# 构建 / 输出
# -------------------------
# 构建产物
dist/
build/
.out/
.next/
.out/
.out-public/
.storybook/
storybook-static/

# 小程序 / 临时编译缓存
miniprogram_npm/
unpack_temp/

# 测试覆盖 / 结果
coverage/
.nyc_output/
test-results/
frontend/test-results/
playwright-report/

# -------------------------
# 系统 / IDE
# -------------------------
# 操作系统临时文件
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# 编辑器 / IDE
.vscode/
.idea/
*.swo
*.swp
*~

# -------------------------
# 临时 / 工具
# -------------------------
tmp/
temp/
*.tmp
*.bak
*.backup

# PM2
.pm2/

# Claude/Serena 缓存 / 备份（如果项目使用）
.claude/
.serena/
.backups/

# -------------------------
# Docker / CI
# -------------------------
# 局部 docker / kube 配置（不要把明文凭证提交）
.env.docker
.dockerignore

# -------------------------
# 常见前端依赖 / 其它
# -------------------------
bower_components/
.sass-cache/

# -------------------------
# 额外说明（保留）
# -------------------------
# - 推荐提交 package-lock.json / yarn.lock / pnpm-lock.yaml（用于 CI 的 npm ci / 缓存）
# - 如果你确实不想把 lock 文件提交，请在此处取消注释相应一行（但不推荐）
